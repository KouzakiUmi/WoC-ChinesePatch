# 《Winds of Change》英文原版地图 — 反抗军总部（Rebel HQ）文字标签抄录

来源文件：`tl_work/backup_maps_original_2026-09-22/RebelHQMap.png`
读取校验：sha256 = 54b75fdee4f9493365a8b982c4a82729429146346e9c20fa6752844c2abbc010（两次读取一致，画面为羊皮纸风格基地平面图，非字幕/书页，无串图）

## 标签清单

- 中上偏右（WAR TABLE 框图上方，标示顶部大长桌） | `WAR TABLE` | 作战桌 / 战争会议桌
- 中部（中央竖向房间组图标左侧） | `CREW QUARTERS`（分两行：第一行 `CREW`，第二行 `QUARTERS`） | 船员舱 / 成员宿舍
- 左中（左侧竖井/隧道图示右边，横排单行） | `TUNNELS`（复数，非单数 `TUNNEL`） | 地道 / 隧道网
- 右侧（右侧竖向房间组图标右边，分两行） | `PERSONAL QUARTERS`（第一行 `PERSONAL`，第二行 `QUARTERS`） | 私人房间 / 个人房间
- 中下（底部舱体/大门图示上方，横排单行） | `MAIN ENTRANCE` | 主入口
- 左下偏中（左下竖井底部图示右侧，横排单行） | `EXIT` | 出口
- 左下角（画框内的信纸） | 花体/手写连笔，**看不清**，无清晰可读单词 | —（推测：一封手写信/便条的装饰性内容，推测）
- 右上角（华丽画框内） | 空白，无文字 | —
- 左上角（纹章/徽记） | 图形徽记，无文字标签 | —
- 顶部中央（罗盘/太阳纹饰） | 装饰图案，无文字标签 | —

## 裁决依据

- CREW QUARTERS 是否存在：是（原文全样为两行大写 `CREW` / `QUARTERS`，即 `CREW QUARTERS`；位置在地图中部偏右、房间组图标左侧）。不存在 `MEMBER QUARTERS` 写法——全图未出现该串。
- “私人房间 / 个人房间”对应原文：`PERSONAL QUARTERS`（右侧，两行 `PERSONAL` / `QUARTERS`），不是 `PRIVATE ROOM` 之类写法。
- `TUNNEL` / `PASSAGE` 是否存在：
  - `TUNNELS`（复数）**存在**，位于左中部，原文即 `TUNNELS`；
  - `PASSAGE` / `PASSAGES` **不存在**，全图无此词；中文“地道”应对应 `TUNNELS`。
- 其余标签英文原文：`WAR TABLE`（作战桌，中上偏右）、`MAIN ENTRANCE`（主入口，中下）、`EXIT`（出口，左下偏中）。
- 看不清的文字：仅左下角画框内的手写花体信件内容不可读（推测为装饰性手写信件，非地图标签）；右上画框为空白。其余 6 个标签均为清晰大写衬线字体，无推测成分。
