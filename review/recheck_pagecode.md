旧版：左页码=`~1~` 右页码=`~2~` 其他文字=无（条带内除两处页码外没有任何署名、脚注或其他文字，仅有书封/书口的装饰卷草纹与羊皮纸纹理）
新版：左页码=`~1~` 右页码=`~2~` 其他文字=无（同上，除两处页码外无任何其他文字）
判定：页码未改
证据：两张裁片均逐字读取并复读一次确认，sha256 不同且与文件一一对应——旧版 `_pagecheck_old.png` sha256=42b57360d38317c9da76ddffa8158f7171cb20d363f4d588766eb2def7b9309b（2360x180，432431 字节，透明背景以棋盘格显示）；新版 `_pagecheck_new.png` sha256=8e6ba4cf4872dd52676c2b537136ece2e553c14d3dafd4a0e3ef4a9cab706ce8（2360x180，327864 字节，透明背景以黑底+青色噪点显示），两次读取同一文件 sha 一致、无串图。字形描述：旧版左页码为"波浪号包层数字1"（数字1左右两侧各是一笔中间下凹、两端上翘的短弧线，明显是 `~` 而非平直短横），右页码为"波浪号包层数字2"；新版左页码同样为"波浪号包层数字1"，右页码同样为"波浪号包层数字2"，两处字形与旧版逐像素观感一致，均不是"-1-/-2-"那种平直短横线包层数字的样式。因此新旧版本页码样式均为波浪号（tilde）样式，未发生变化。
