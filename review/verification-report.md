# 图片修复回归核验报告

> 第三轮制作更新：§7 的 A-6 与 b0009 连接词已落实，当前文件与像素检查见 [第三轮图片修复记录](image-repair-round3-completion.md)。保留下方第二轮的 FAIL / PARTIAL 作为历史验收记录，等待独立复核本轮结果。

> 2026-09-26 后续复核与修复：HQ 已整图回滚；本报告的“船员舱”、`b0009` 署名漏译及整页重绘不合要求等结论已撤销或修正。教程透明度、截图英文残留、书外青绿残色及追加发现的书页错译均已处理。当前结果以 [图片修复完成记录](image-repair-completion.md) 为准；下面保留原报告作为历史审查记录。

- 范围：`v1.3.8`（commit `fe8d825`）对 32 张图片的修复 —— 27 张修图 + 5 张脚本重渲染字幕
- 方法：① 4 路干净上下文子代理逐张读图核对文字（PASS/FAIL）② 3 路新旧全文比对（工单内/工单外）③ 我做的像素级指标分析（alpha / 强差异 / 可见区）
- 数据：`review/verify_*.md`（文字核验）、`review/diff_*.md`（新旧比对）、`review/image_metrics.tsv`（像素指标）

---

## 1. 文字修复：全部通过

| 核验组 | 结果 | 报告 |
| --- | --- | --- |
| 地图 6 张（8 项核对 + 6 张整体） | **PASS 14 / FAIL 0** | `review/verify_maps.md` |
| 教程图 4 张 | **PASS 5 / FAIL 0** | `review/verify_tutorials.md` |
| 书页 A 级 20 条（含 §6.4 图题↔书目 8 张） | **PASS 20 / FAIL 0** | `review/verify_booknotes_A.md` |
| 书页 B/C 级 10 条 | **PASS 10 / FAIL 0** | `review/verify_booknotes_BC.md` |
| 开场字幕 5 条（zz11/zz12/zz4/zz2/zz10） | 本会话直接改 JSON+重渲染，已目检 | 见主报告 |
| **合计** | **49 PASS / 0 FAIL** | |

核验时同步确认：无新引入错别字、无压字/截断、无行距错乱、无关标签未被误改。
`tut005` 右侧小地图原本残留的英文 `…ICROWN` **已中文化为「东冠」**（工单里这项是可选的，实际做了）。

---

## 2. 技术回归：第一轮发现 3 项（**第二轮均已修复并验收**，详见 §7；另有 2 条我方误报已在别处更正）

### 2.1 ✅ 教程图 alpha 通道丢失（4 张）—— **第二轮已修复，并经你方核查确认无问题（PASS 关闭）**

| 文件 | 旧版 | 新版 | 影响 |
| --- | --- | --- | --- |
| `tut001.png` | RGBA，**34.9% 全透明 + 60.7% 半透明(a=230)** | **RGB，无 alpha** | 半透明教学叠层变成不透明 |
| `tut003.png` | RGBA，22.5% 全透明 + 63.1% 半透明(a=204) | RGB | 同上 |
| `tut005.png` | RGBA，11.3% 全透明 + 61.9% 半透明(a=217) + 18.6% 不透明 | RGB | 同上 |
| `tut006.png` | 同 tut005 | RGB | 同上 |

- 旧版是**压在游戏画面上的半透明蒙版**（整块面板 alpha≈204–230，外加不透明的截图区与透明边距）；新版被拍平成 RGB 后，**进游戏会遮住背景**。
- 修复建议（二选一）：
  1. **回填旧 alpha**：旧图 alpha 是"面板值 + 不透明截图区 + 透明边距"的三段结构，几何未变（新版强差异包围盒仅落在文字区），把 v1.3.7 的 alpha 通道原样贴回新版 RGB 即可，属低风险机械操作；
  2. 让修图 agent 重出 PNG 时**保留 alpha**。
- 另注：6 张地图也从 RGBA 变 RGB，但旧版本来 100% 不透明，**无视觉影响**，可不处理（Ren'Py 不敏感）。

### 2.2 ✅ 书页绿幕键控残留（青绿 matte）—— **第二轮已全部清零（17 张 = 0 像素）**；下表为第一轮实测，留档

| 文件 | 青绿像素 | 占画布 | alpha 中位（不透明度） |
| --- | --- | --- | --- |
| `b0002.png` | 93,563 | **10.15%** | **19 / 255 ≈ 7%**（最大 115） |
| `b0008.png` | 16,644 | 1.81% | 18 / 255 |
| `b0004.png` | 7,720 | 0.84% | 19 / 255 |
| 其余书页 | 238–712 | ≤0.08% | 同上 |
| **旧版 v1.3.7 全部** | **0** | 0% | — |

- **成因**：抠绿幕时半透明边缘保留了绿色（`g>r+25 且 b>r+20`），这些像素在旧版里是 0。
- **⚠️ 严重度校正**：目检报告里的"**大片饱和青绿实心区**"是**预览工具丢弃 alpha 后的假象**——实测这些像素 alpha 中位仅 19（≈7% 不透明度），游戏内只会呈现为一层**淡青绿色薄雾**（b0002 覆盖约 10% 画布），并非实心色块。
- 修复：把这些像素的 RGB 换成页面底色，或直接把 alpha≤25 的区域归零。工作量小。

### 2.3 ✅ 原工单里的一条误报

- **`b0002` 页码**：工单 C 级称"旧版为半角 `-1-`，应改 `~1~`"——**经新旧两版裁片逐字复核（`review/recheck_pagecode.md`），两版均为 `~1~/~2~`，本来一致，无需修改。** 该条从工单撤下。

### 2.4 🟠 书页 17 张是"整页重绘"，不是"只改文字"

| 图组 | 强差异像素（阈值>60） | 差异包围盒 | 判定 |
| --- | --- | --- | --- |
| 6 张地图 | 0.48–1.07% | 落在标签区 | ✅ 定点编辑 |
| 4 张教程图 | 3.40–7.45% | 落在文字区 | ✅ 定点编辑 |
| **16 张书页**（b0013 除外） | **36–42%** | 全画布 | ⚠️ 整页被重画 |
| `b0013.png` | 7.33% | — | 接近定点编辑 |

- 书页可见区（双不透明像素）平均绝对差 **12–32 灰阶**，新版整体略暗（如 b0011 可见均值 174,140,88 → 162,127,79）。
- **性质说明（据并行复核 `review/image-current-recheck.md`）**：本次修图是**你方授权的"用英文原图重新生成"**，因此"整页重绘"本身**不算违规**——早期工单里"只改指定字符串、其余像素不动"的要求已被该授权取代。
- 但重绘带来的**客观影响仍需处理**：① 纸纹/边框/装饰/插图整体变化，建议做一次**美术走查**（与 v1.3.7 逐张对比）；② **错误面被放大**——本次 149 处工单外差异中混入了 `b0014/b0015/b0008` 等**真错**；③ 重绘管线还引入了 §2.1（alpha 丢失）与 §2.2（青绿残留）两类技术缺陷。
- 好消息：文字层经 4 路核验 49/49 全对，未见新错别字或排版事故（**注意：49/49 只覆盖原工单指定项，不等于全篇译文通过**——重绘引入的语义问题由 §3.3 的英文对照判定覆盖）。

### 2.5 ✅ 已核验合规的部分

- `manifest.json`：`original` 字段 **0 处被改**（英文原版哈希完好）；32 张图 sha256/size 已同步，`payload` 68 项自检全绿
- 提交 `fe8d825` 只含预期文件（README×2、manifest、32 张 PNG），version 1.3.7→1.3.8、升级提示同步更新
- 54 张 PNG 全部可正常解码，尺寸均 1280×720（主菜单除外，未动）

---

## 3. 工单外改动（新旧全文比对）

**总体：文字修复本身 49/49 通过，但修图过程产生了大量工单外改动。**

| 图组 | 工单内 | 工单外 | 质量问题 | 报告 |
| --- | --- | --- | --- | --- |
| 地图 6 + 教程 4 | 13 | **23** | 3 | `review/diff_maps_tutorials.md` |
| 书页 b0001–b0009 | 36 | **78** | 8 | `review/diff_booknotes_1.md` |
| 书页 b0010–b0017 | 26 | **48** | 8 | `review/diff_booknotes_2.md` |
| **合计** | **75** | **149** | **19** | |

> 工单外改动是本次修图的**主体而非例外**：书页 126 处、教程/地图 23 处。成因是修图方**从英文原图整体重制**（见 §2.4 的像素证据；该重制方式系你方授权），而非"定点改字"。

### 3.1 🔴 必须回滚/修复

| # | 图 | 问题 | 证据 |
| --- | --- | --- | --- |
| 1 | `tut005` 底部两对话框、`tut006` 中部对话截图 | **中文回退成英文**（严重回归） | 旧="这段队伍闲聊会受到你选择保留佩雷格里诺酒馆的影响。"/"当然。我们会一起面对一切。" → 新="This party banter is affected by…"/"Of course. We'll handle everything together."；独立复核 `review/recheck_tutorial_english.md` 判定**成立** |
| 2 | `ValinorthMap` | "瓦利诺斯森林"标签**右移 20–30px**、下沉 20–25px，与"瓦利诺斯村"间距 60px→30px | 复核报告；文字未变，属错位 |
| 3 | 8 张书页（b0013/b0014 最明显） | 书**上缘/右缘青绿残留条带** | `diff_booknotes_2.md`；与 §2.2 同源 |
| 4 | 教程 4 张 | **alpha 丢失**（见 §2.1） | `image_metrics.tsv` |
| 5 | `b0009` | ~~右页署名「~迈勒斯」被删~~ → **已撤销（我方误报）** | **英文原图右页末段下方本就没有署名**，只有页码 `~2~`；`~迈勒斯` 是旧中文版额外添加的。新版末段与英文逐点相符（"旅途中不妨顺道去看看"= `Feel free to stop there on your journey`），旧版"没人会阻止你"反而是增译 → **新版更准，无需补署名**。证据 `review/en_b0009_signature.md` |
| 6 | `b0002` | **青绿残留**（经像素校正，实际为淡雾而非实心块，见 §2.2）：10.15% 画布有青绿 RGB，但 alpha 中位仅 19（≈7% 不透明度） | `review/recheck_b0009_b0002.md`（目检判"重"，系预览丢 alpha 的假象）+ `image_metrics`/像素实测；其余项正常：人名"阿莱斯蒂亚"×4、署名"维勒尔"、无英文残留 |

### 3.2 🟠 需要定夺（工单外，但未必错）

| # | 图 | 改动 | 我的判定依据 |
| --- | --- | --- | --- |
| 5 | `RebelHQMap`（**已被并行流程整图回滚，需裁决**） | 见 §3.2b |
| 6 | `tut005` | 纯洁与**堕落** → 纯洁与**腐化**（标题+4 处正文） | 对白 `0015#5732/#5735` 作"纯洁与腐化"（2 处），"纯洁与堕落"0 处 —— **方向正确**，把图片拉回对白基准，建议保留并补进 `terms.tsv` |
| 7 | `b0011` 正文 | 德拉库 → **德雷库**（工单只点名 b0015） | 与 `names.rpy` 基准一致，**方向正确** |
| 8 | 教程 4 张 | 工单外措辞漂移 19 处（tut001×7、tut003×2、tut005 标题术语、tut006×5） | 系从英文原图重制所致；判定见 §3.3（新更准 7 / 旧更准 2 / 皆可 14） |

### 3.2b ✅ `RebelHQMap` 回滚 —— **裁决：回滚正确，问题在文本侧**

**你的裁定**：`crew` 也有"团队成员"之意，陆上总部没有"船员"，`crew quarters`→**船员舱是文本侧的错误翻译**；地图回滚到 v1.3.7（成员宿舍 / 个人房间 / 地道）是**对的**，工单 A-5 **撤销**。

**英文原图事实**（`review/en_rebelhq_labels.md`，两次读取哈希一致）：中部 `CREW QUARTERS`（**无** `MEMBER QUARTERS` 写法）、右侧 `PERSONAL QUARTERS`、`WAR TABLE` / `TUNNELS` / `MAIN ENTRANCE` / `EXIT`。→ 英文原文就是 `crew quarters`，错的不是英文，是**把它按字面译成"船员舱"**。

**文本侧涉及面：`船员舱` 共 16 行，逐行按场景（map 块内英文关键词 + 邻句）判定：**

| 判定 | 行数 | 明细与依据 |
| --- | --- | --- |
| 🔴 **总部语境 → 应改为「成员宿舍」** | **9** | `0006#2115`（同句有 `war table`）、`0007#2671` `#2685` `#2737`（总部参观，map119/120 总部词 2–4、船词 0）、`0008#2838`（map129 块内明写 *"This HQ is a pretty nice place"*）、`0008#3127`（map137 总部3/船0）、`0009#3249` `#3366`（map138/140 总部3、5，作战室+挖掘）、`0010#3732`（会议在作战桌开、`We'll set sail in three hours` 前被分到房间） |
| ⚪ **船上语境 → 保留「船员舱」** | **7** | `0003#937` `#970`、`0007#2417`（紧邻 `0003#938`"我下到**船上**的船员区"）、`0011#4298`（"这种'海上生活'"）、`0011#4366`（"抵达巴尔泰乌斯""海盗们快启航"）、`0020#7922`（EN `center of the ship`）、`0022#8643`（前句"我们望着大海"） |

> 另有"船员"单用 **15 行**，全部是船/海盗语境（`ships and crew`、`his crew`、`crew area of the ship`…），**译法正确，不动**。

**建议改法（9 行，只动 TSV 最后一列）**：`船员舱` → **`成员宿舍`**、`船员舱室` → **`成员宿舍`**（与地图标签统一）。
- `0009#3249` `一道明亮的白光从船员舱室中迸发` → `…从成员宿舍中迸发`
- `0009#3366` `回船员舱室去，福泰姆。` → `回成员宿舍去，福泰姆。`
- `0007#2737`（教程提示）`每位已解锁的角色在船员舱都有一个心灵对话事件` → `…在成员宿舍都有…`

#### ✅ 已执行（本轮，`tl_work` 源侧，共 87 行）

| 规则 | EN | 改动 | 行数 |
| --- | --- | --- | --- |
| `crew` | `crew quarters`（陆上总部） | 船员舱 7 + 船员舱室 2 → **成员宿舍** | 9 |
| `tunnel` | `tunnels` | 隧道 → **地道** | 68 |
| `pq` | `personal quarters` | 私人住处 2 + 私人房间 2 → **个人住所**（裁决定词） | 4 |
| `priv` | `private room(s)/quarters` | 私人的房间 2、私人居室 2、私人寝室 1、私人住处 1 → **私人房间** | 6 |

- **船上 7 行 `crew quarters` 保留「船员舱」**：`0003#937` `#970`、`0007#2417`、`0011#4298` `#4366`、`0020#7922`、`0022#8643`
- 备份：`review/backup_chunks_termfix_20260926-112223/chunks/`（crew+tunnel）、`…_20260926-120605/chunks/`（pq+priv）
- 校验：两批共 87 行、**非译文列改动 0 行**、行数一致、断列仍为已知 3 条、`fantasy_check` 仍 111 条
- 构建状态：⚠️ **只改了源**（按你的指示），`payload/…/script.rpy` **尚未重建**，待文本批改一起 `build → compile → verify`

#### 📐 最终术语方案（按你的三次裁定）

| EN | 中文 | 当前分布 |
| --- | --- | --- |
| `crew quarters`（陆上总部） | **成员宿舍** | 对白 9 行 + 地图 1（RebelHQMap） |
| `crew quarters`（船上） | **船员舱** | 对白 7 行 |
| `personal quarters` | **个人住所** | 对白 4 行 + 地图 1（RebelHQMap 现为"个人房间"，**待改图**） |
| `private room(s)/quarters` | **私人房间** | 对白 18 行 + 地图 2（Castle/Coli，英文原词核验中） |
| `tunnels` | **地道** | 对白 71 行 + 地图 1 |
| `living quarters` | 起居空间/营房（按语境，不动） | 对白若干 |

**剩余地图侧待办**：
1. `RebelHQMap` 标签「个人房间」→「**个人住所**」（1 处，图片项）——英文原词 `PERSONAL QUARTERS`，已定为"个人住所"
2. ~~`AlarinMapCastle`/`AlarinMapColi` 是否需改~~ → **✅ 已核验，不用改**：两张图英文原词均为 **`PRIVATE ROOM`**（单数，非 PERSONAL），中文「私人房间」正确。证据 `review/en_castle_coli_labels.md`（含完整标签清单；注意英文原版路径是 `backup_additional_text_images_2026-09-22\AlarinMapCastle.png`，`backup_maps_original` 下没有该文件）

**术语规则已入库**（`review/terms.tsv` 新增 3 条）：`personal quarters→个人住所`、`private quarters→私人房间`、`tunnel→地道`；`crew quarters` 因语境二义只写注释不写自动规则。回归校验：`review_tool terms` 待改写 **0 行**、`scan` 回到基线 **85 条**（原 85，未引入新问题）。

### 3.3 语义级改动判定（对照英文原文）

**教程 4 张**（`review/semantic_tutorials.md`，23 处差异）：**新更准 7 / 旧更准 2 / 皆可 14 / 都不准 0**
- ✅ `tut005` 英文原题 **"Purity and Corruption"** → 新版「纯洁与**腐化**」正确；旧版「堕落」既不符英文也不符对白基准（`dialogue_0015` 2 处"纯洁与腐化"）
- ⚠️ 判**旧更准**的 2 处，其中两处正是**我工单里的 C 级建议**：`C-3`「适用地图→相应地图」（applicable=适用，旧更准）、`C-4`（tut006"完成角色的…"被改写成"与角色完成…"，`do a character's heart-to-hearts` 旧更准）——**我的建议欠准，按复核结论回退更稳妥**
- ✅ `tut005` 右侧小地图 `…CROWN` → 「东冠」，与正文地名一致（旧版反而是英文残留）

**书页 b0014–b0017**（`review/semantic_booknotes_2.md`，6 条语义改动）：**新更准 2 / 旧更准 2 / 皆可 1 / 1 条为复合判定**
- 🔴 **b0014 新译把范围搞反**：英文 `Disposing of criminals before making them serve sentences… was barbaric` 只说"处决"野蛮，新译用"**都**"把"服刑、常规司法"也并进"被视为野蛮"，**与原文相反** → 应回退或重译
- 🔴 **b0015 新译语义偏移**：`keeps most of his subjects happy`→旧"保持快乐"逐字正确；新"安于现状"偷换成政治态度，且与下一段 `Many are less inclined to insurrection` 重复、削平递进 → 建议回退
- ✅ b0016 两处新更准（补回 `to the death`、补译 `potential`），b0017 新更准（旧译 `tending`→"试图接触"是错的）
- ➖ b0016「斗士→勇士」4 处：两者皆可（术语风格差异）

**书页 b0006–b0009**（`review/semantic_booknotes_1b.md`，42 条工单外改动）：**新更准 5 / 旧更准 3 / 皆可 32 / 都不准 2**
- ✅ 新更准 5：`b0007` 句首**旧译把英文唯一理由反着翻**（EN 只说"身体承受不住酷寒"，旧"并非因为胆怯"直接否定它），新版修好；`b0006` 补回 `corresponding=通信`；`b0008` 错字"承循→遵循"；`b0009` 两处补回旧译漏掉/编造的信息
- 🔴 旧更准 3：`b0009` 把 `the blade` 译成"武器"（丢剑的信息）、`Zamira` 一句把"在郊外会面"改成"带他去郊外"（凭空加陪同动作）；~~署名「~迈勒斯」缺失~~ **（该项已撤销：英文原图本无落款，见 §3.1 第 5 项）**
- 🔴 **都不准 2（都在 b0008，需重译）**：① `what was observed on the night of the most recent generation` —— 旧把"那夜"扩大成"一代中"，新把无施事被动译成"最近一代人观察到的"（主客易位），**两版都漏了 `on the night`**；② `We must simply take each day as it comes` —— 两版都译成"认真对待"（原意"顺其自然、一天天过好"），新还丢了"每天"
- 质量：4 张无英文残留/错别字/压叠，页码均 `~1~`；署名 `b0006/b0007/b0008` 齐全

**书页 b0002/b0003/b0005**（`review/semantic_booknotes_1a.md`，27 条工单外改动）：**新更准 1 / 旧更准 2 / 皆可 23 / 都不准 1**
- ✅ `b0005` 右页新更准：英文 `a death is coming to Valinorth` 的**确定项/存疑项**在旧译里被颠倒，新译修回
- 🔴 `b0005` 右页末句 **都不准**：两版都把 `a thing of the past`（不复存在）译成"对过去的确切事物/认知" → 需重译
- 🟡 旧更准 2（**方向更正：是新图错了，旧图对**）：① `b0002` EN `merely`＝"只是"，**新图曾错作"大多"**（把限定放宽）→ **第二轮已改回「仅仅建立在个人的信仰和确信之上」，复核 PASS**；② `b0005` EN `often`，**新图曾把"常常"整词删掉**（频率信息丢失，旧图"先知可以常常非常生动地回忆起"更准）→ 第二轮是否补回，复核中（`verify_round2_booknotes_1.md` 第 13 项）
- 质量：三张新版无英文残留/错别字/压叠，署名齐全；`b0002` 书外青绿斑块、`b0003/b0005` 顶缘轻微（见 §2.2）

**并行流程的补充发现**（`review/image-current-recheck.md`）：
- `b0001` 三处：询问对象被换、`take it to my grave` 增译、`was` 时态变现在时
- `b0004`：`lash out` 的"突然发难"→"任由其发展"（**反义**）；`bend to their will`→"偏离我们的意志"（**反义**）
- `b0007`：`the reach of this energy is not infinite`（影响范围有限）→"能量并非无穷无尽"（储量有限）

### 3.4 待办（语义判定已全部完成）

- ✅ b0001–b0005（`semantic_booknotes_1a.md`）+ 并行报告补 b0001/b0004/b0007
- ✅ b0006–b0009（`semantic_booknotes_1b.md`）、b0014–b0017（`semantic_booknotes_2.md`）、教程（`semantic_tutorials.md`）
- ✅ `b0002` 独立复核（人名/署名/无英文残留正常；青绿按像素校正为 §2.2）
- ✅ `b0009` 署名 **撤销**（英文原图本无落款）；`b0002` 页码 **撤销**（两版都是 `~1~`）
- ✅ **`RebelHQMap` 已裁决**：回滚正确，改文本侧（见 §3.2b）
- ✅ ~~**待执行的回退/重译**~~：`b0014`、`b0015`、`b0008`×2、`b0005`×1、`b0001`×3、`b0004`×2、`b0007`×1、`b0009`×3、`b0002`×1、`b0005`×1（旧更准）→ **第二轮全部修复并验收**（`verify_round2_booknotes_1.md` 13/13、`_2.md` 8 PASS + 1 PARTIAL→第三轮补完）；`tut003 C-3`/`tut006 C-4` 也已按工单落地（教程 11/11）
- ✅ ~~**待你决定**~~：① 9 行 `船员舱`→`成员宿舍`、② `私人房间/个人房间` 与 `地道/隧道` 两处一致性 —— **均按你的三次裁决执行完毕**（87 行术语 + 后续细化，见 §3.2b 与 §8）

---

## 4. 文本侧剩余问题

图片之外的部分**一条未改**（今日复扫数字与校对时逐字一致），详见 `review/remaining-issues.md`：

- **A 68 / B 313 / C 198 = 579 条**，其中最急的是**成品缺 3 行台词**（0003#1196、0021#8162）与 `0013#4911` 括号
- 术语族未收敛：萨卢斯 11、女先知 10、中央广场 6、相位 13、办公室 13、羁绊 24、能量族 41 等

---

## 5. 交付前动作（**第二轮图片修复后已更新**）

1. ~~图片 P0：教程英文回退~~ → ✅ **已修**（11/11 PASS）
2. ~~图片 P0：教程 alpha 丢失~~ → ✅ **已恢复**，但**新增 P0**：原透明边距变半透明黑（tut006/tut005/tut003）→ 见 §7 与工单"待修 1"
3. ~~裁决 `RebelHQMap`~~ → ✅ 已裁决（回滚正确），但**执行 A-6 未做**（个人房间→个人住所）→ 见 §7
4. 书页：青绿已清零、后半语义 8 PASS；剩 `b0009` 一处 `Instead` 连接词（PARTIAL）+ 前半六张验收中
5. 重装补丁（游戏目录仍是旧图，`verify` = **36/68**）
6. 文本侧按 `remaining-issues.md` §6 顺序推进（**87 行术语已改源，尚未 build**；其余 579 条）

---

## 6. 核验产物索引

| 类别 | 文件 |
| --- | --- |
| 文字核验（PASS/FAIL） | `verify_maps.md`、`verify_tutorials.md`、`verify_booknotes_A.md`、`verify_booknotes_BC.md` |
| 新旧全文比对（工单内/外） | `diff_maps_tutorials.md`、`diff_booknotes_1.md`、`diff_booknotes_2.md` |
| 语义判定（对照英文） | `semantic_tutorials.md`、`semantic_booknotes_1a.md`（b0002/3/5）、`semantic_booknotes_1b.md`（b0006–9）、`semantic_booknotes_2.md`（b0014–17） |
| 争议裁决（英文原图） | `en_rebelhq_labels.md`、`en_b0009_signature.md`、`recheck_tutorial_english.md`、`recheck_b0009_b0002.md`、`recheck_pagecode.md` |
| 量化数据 | `image_metrics.tsv`（27 张）、`image_metrics_round2.tsv`（32 张 vs v1.3.7） |
| **第二轮复核** | `verify_round2_tutorials.md`、`verify_round2_maps.md`、`verify_round2_booknotes_1.md`、`verify_round2_booknotes_2.md` |
| 并行流程报告 | `image-current-recheck.md`（HQ 回滚）、`image-repair-completion.md`（第二轮修复完成记录） |
| 上游文档 | `image-fix-report.md`（施工工单，**含★第二轮待办**）、`fantasy-proofread-report.md`（主校对报告）、`remaining-issues.md`（剩余清单） |

---

## 7. 第二轮图片修复复核（2026-09-26 第二次）

**本轮变更范围**：23 张图（17 书页 + 4 教程 + `ValinorthMap` + `RebelHQMap`），`manifest.json` 只改这 23 条的 `sha256/size`（46 行），**`original` 字段 0 改动**，version 仍 1.3.8，`patch_tool check` = **OK (68)**。制作方记录见 `image-repair-completion.md`（13 张经图像生成重制、7 张仅清残色、alpha 为**重建**而非套用原图）。

### 7.1 验收结果

| 组 | 结果 | 报告 |
| --- | --- | --- |
| 教程 4 张（文字 11 项） | **PASS 11 / FAIL 0** | `verify_round2_tutorials.md` |
| 地图 2 张（7 项） | **PASS 6 / FAIL 1**（A-6 未做） | `verify_round2_maps.md` |
| 书页后半（b0008/b0009/b0014/b0015 + 抽查 b0016/b0017） | **PASS 8 / FAIL 0 / PARTIAL 1** | `verify_round2_booknotes_2.md` |
| 书页前半（b0001/b0002/b0004/b0005/b0006/b0007 + 副作用 6 张） | **PASS 13 / FAIL 0** | `verify_round2_booknotes_1.md` |

### 7.2 技术指标（`image_metrics_round2.tsv`）

| 指标 | 结论 |
| --- | --- |
| **青绿键控残留** | ✅ 17 张书页**全部归零**（b0002 93,563 → 0；b0008 16,644 → 0） |
| **教程 alpha** | ✅ 已恢复 RGBA（tut001 `34.7/25.8/39.5`、tut003 `22.2/5.6/72.2`、tut005 `10.4/23.1/66.5`、tut006 `5.3/23.5/71.2`；原版分别 `34.9/2.2/62.9`、`24.1/3.1/72.9`、`12.6/19.0/68.4`、`12.6/19.7/67.7`） |
| **教程边距/透明度** | ✅ **PASS（你方核查通过，关闭）** —— 我方曾据"白底合成后原透明区偏暗"（tut006 RGB93、tut005 182、tut003 241、tut001 255；"原透明→现不透明"像素 87,567/39,548/17,264/1,610）提出疑虑，**经你核查确认透明度无问题**，不再作为待办；透明度为本轮重建值，属既定制作方式 |
| 书页 vs v1.3.7 | 强差异 36–42%（仍是整页重制），**合成后**差异 4–9.6%（含文字修正的预期变化） |
| 地图 vs v1.3.7 | ≤1.1%（仅标签改动）；`RebelHQMap` **0.0%** = 仍是回滚版，A-6 未做 |

### 7.3 第二轮剩余待办 —— **第三轮已全部关闭**

1. ~~教程边距/透明度~~ → ✅ **你方核查通过，已关闭**
2. ~~A-6 `RebelHQMap`「个人房间」→「个人住所」~~ → ✅ **第三轮已修 + 独立验收 PASS**（`verify_round3_images.md`：标签确为「个人住所」，其余 5 标签逐字无误；像素差异仅标签区 1155 px）
3. ~~`b0009` 句首 `Instead`「因此」→「反而」~~ → ✅ **第三轮已修 + 独立验收 PASS**（保留那柄剑/深夜/郊外会面/索尔伯格授权，右页无署名）
4. ~~`b0005` EN `often`~~ → ✅ **PASS**（现图「先知**往往**能清晰地回忆起」）

**图片侧文字类问题：全部关闭。** 第三轮验收合计 **PASS 14 / FAIL 0**。

---

## 8. 文本侧修复与构建记录（第三轮 · 文本/代码轮，2026-09-26）

### 8.1 修复量：**873 行译文改动**（权威值 = 相对 `backup_chunks_termfix_20260926-112223` 的去重行数；主批次 812 已与 payload 差异行数完全对账、差额 0）

> 核算方法：逐行比对当前 `tl_work/chunks` 与当日基线；每次 `build` 后 payload `.rpy` 差异行与 chunk 改动数**差额为 0**，说明 build 未引入任何额外改动。
> 各操作自报数（部分行在多个批次中被先后触及，故自报合计与实测有出入，**以实测为准**）：

| 批次 | 操作 | 自报行数 |
| --- | --- | --- |
| 结构硬伤 | `fix_critical_rows`（缺行并回 + 括号） | 3 |
| 术语族批量 | `apply_terminology` 4 次（crew+tunnel 77 / pq+priv 10 / 萨鲁斯·公事房等 51 / 灵性力量 7） | 145 |
| 逐条工单 | `apply_worklist_changes`（30 份 chunk 工单） | 564（含 16 行标签回补） |
| 同源行同步 | `dedupe_analysis`（70 组同人同英文 → 统一） | 54 |
| 手工批次 | `apply_manual_fixes`（丢人名、半角引号、豪尔敬语、空甲、习语回改…） | 22 |
| 零散 | fixups 1 + 毋庸→毫无 2 + 重复块同步 1 + 缺逗号 3 | 7 |
| **尾部工单** | `tail_howl_honorifics` 19 + `tail_armor_energy` 24 + `tail_bonds` 21 | 64 |
| **实测合计（按行去重）** | | **873** |

### 8.2 质检（全绿）

| 检查 | 结果 |
| --- | --- |
| 结构 | 断列 **0**；`{}` 标签与 `[]` 插值不一致 **0**；行数 26×402 + 0026=370 |
| `fantasy_check` | **2 条**，均为 `0020#7896` 有意删除键号 `Damek: z01605`（已文档化的唯一例外） |
| `review_tool scan` | **18 条，全部 [info] engine-string（界面串）**；[high]/[term]/[low] **全 0**（本轮把 4 类 low 级误报清零：`repeated-char` 的"目的的/不了了"、`length-ratio` 的标签与中英密度、`ascii-residue` 的界面技术词、`halfwidth-punct` 的 `%H:%M`）；另实测**对白含英文单词 0 行** |
| `terms` / `dedupe` / `register` | 待改写 **0** / 需统一 **0** / 语域命中 **0**（原 4 条子串与教程误报已由代码修复消除） |

### 8.3 构建链（按 §5 流程执行）

1. `chain_build.py --apply`（本次统一入口，等价于 workflow §5）：
   1. **挂起检查**：只认"当前值==基线值"的真·未应用行 → **0 条**（与清单有差异的旧行属后续改进，不会被回退）
   2. `tl_tool.py build --tl-dir tl_work --game-root payload` → 写 `payload/game/tl/chinese/*.rpy`；**每次 build 的差异行数都与 chunk 改动数差额 0**
   3. 编译：暂移 `payload/game/screens.rpy`（payload 非完整工程，`gui.language` 依赖会报错）→ `WindsofChange.exe payload compile` **退出码 0** → 5 个 `.rpyc` 更新 → 复原 `screens.rpy`
   4. `update_manifest_hash.py --apply` → 定点更新（首轮 8 条、尾部轮 7 条），`original.*` 与 version 未动（`original` 字段 HEAD/现 均为 56），CRLF + 3 空格缩进保持
   5. `patch_tool.py check` → **Payload hashes: OK (68)**（本轮新增的哈希校验）
2. 游戏目录 `verify` = **28/68**（文本与图片均已更新，**待你决定是否重装**）
3. ⚠️ 备注：`.rpyc` 的 sha **每次编译都会变**（即使 `.rpy` 未变，如 `names.rpyc`），属 Ren'Py 编译产物非确定性；因此**每次编译后都必须同步 manifest**，否则 `check` 会报失配。

### 8.4 代码修复（本轮）

| 文件 | 修复 |
| --- | --- |
| `tools/patch_tool.py` | ① `check` 此前**只查文件存在、不比哈希**，与 README「payload 完整性」承诺不符 → 新增逐文件 sha256 比对；② `install` 写入前校验 payload 与 manifest 一致，不一致即中止（`--force` 可跳过） |
| `tools/review_tool.py` | ③ `exclude_en` 支持 `|` 分隔多词（原单词无法排除匕首/习语语境）；④ `register` 新增 `mark_hit` 语境排除 + `UI_HINT_RE` 教程豁免 → **4 条子串/教程误报归零**（"袭上头顶"命中"上头"、"拒绝了"命中"绝了"、教程行命中"流程"）；⑤ `scan` 的 low 级规则修正：`repeated-char` 加正常语境排除、`length-ratio` 改按去标签后的可见文本与合理阈值（0.15~1.05，中英密度差）、`ascii-residue` 豁免界面技术词、`halfwidth-punct` 豁免 `%H:%M` → low 级从 81 条降到 0 |
| `review/terms.tsv` | ⑤ 新增 `personal quarters→个人住所`、`private quarters→私人房间`、`tunnel→地道`；`blade/sword` 增 `dagger\|put me to\|sword arm` 排除；`crew quarters` 只写注释（语境二义，不设自动规则） |
| `tl_work/*.py`（本轮新增） | `apply_terminology`、`fix_critical_rows`、`gen_worklist`+`apply_worklist_changes`（工单流水线，含**标签守门**）、`repair_tags`（`{w}` 回补）、`dedupe_analysis`（同源同步，今天改过优先）、`apply_manual_fixes`（断言式批次）、`update_manifest_hash`、`round2_*.py` |

### 8.5 已知例外（不计入缺陷）

- `0020#7896`：英文原行 `Damek: z01605` 是**内部键号**，按既定策略删除 → `fantasy_check` 剩余 2 条 A 即此行的键号与 `\n` 差异
- `ascii-residue` / `engine-string 18`：界面串的按键名、渲染器名、文件名、制作人员名按规格**保留英文**
- `punct_check` 英文省略号 4 行：`{w=1.0}.{w=1.0}...` 逐字点读的既定样式
- `gender_check` **5 条冲突均为误报**：4 条是中文按汉语习惯省略代词（EN 有 she/her 但 CN 不需要说出来）；1 条 `0020#7620` 是**英文原文把女性 Vivien 误写成 `he'd`**（EN 其余 5 行均作 she/her，CN 全篇也以女性称呼"薇薇安"）——中文"她"是**对的**，应改的是英文侧
- `variant_check` 7 组：滑动窗口造成的子串误报（"执政官"含"执政官"→执政团、"反抗军内部"含"总部"、斯知/先派 等），`dedupe` 实测需统一 **0 行**
